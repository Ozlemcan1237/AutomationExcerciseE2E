package utilities;

import io.restassured.http.ContentType;
import io.restassured.response.Response;

import java.util.HashMap;
import java.util.Map;

import static io.restassured.RestAssured.given;

public class AuthenticationAutomationExcercise {
//    public static String generateToken() {
//
//        String url = "http://automationexercise.com/";
//
//        Map<String, String> tokenBody = new HashMap<>();
//        tokenBody.put("Email Address", "ozlmm7769@gmail.com");
//        tokenBody.put("Password", "AutomationE2E");
//
//        Response response = given().contentType(ContentType.JSON).body(tokenBody).when().post(url);
//
//        return response.jsonPath().getString("token");
//    }
}
